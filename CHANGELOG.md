# 更新日志

本文档用于记录 Yaya 运营管理平台的重要变更。

## 0.1.5 - 2026-09-17

### 新增

- 新增应用上线申请与审核流程：客户平台可凭有效平台许可证提交应用发布快照，运营端支持审核，已通过应用可供已授权客户查询。
- 新增服务商、提成规则、返利台账和结算批次能力；订单可关联服务商，服务商账号仅可查看所属订单与返利数据。
- AI 员工支持上传头像，并在运营端与公开市场返回可访问的头像地址。
- 新增独立服务商门户入口，支持服务商角色查看订单、返利和结算信息。

### 优化

- 运营管理台重构为按业务域拆分的 App Router 页面与共享运营布局，路由切换不再加载无关业务数据。
- 拆分认证、请求、类型、页面头部、侧栏和通用操作组件，提升页面维护性与数据加载边界。
- Skill ZIP 与 AI 员工头像等运行时文件改为持久化目录管理，容器更新后可继续使用。
- 部署脚本、镜像构建和 Compose 配置完善运行时数据卷、Cargo 镜像配置及旧版数据迁移支持。

### 更新

- 管理台依赖升级并引入 `react-easy-crop`，用于头像裁剪上传体验。
- 导航结构补充应用产品、应用申请、分销、服务商和提成返利入口。
- README、部署说明与 API 概览同步更新至运营平台最新能力。

### 修复

- 修复 AI 员工和 Skill 删除时运行时资源无法清理的问题。
- 修复管理台自身请求进入审计日志，以及反向代理部署时来源识别不一致的问题。
- 修复本地启动与发布过程中的旧进程、依赖镜像重试和历史许可证中心数据库迁移兼容问题。

## 0.1.4 - 2026-08-07

### AI 员工自动版本与包版本

- AI 员工、人格和 Skills 的版本改为系统维护，新建统一为 `1.0.0`，保存编辑时按十进制位自动迭代；`1.0.9` 后为 `1.1.0`，`1.9.9` 后为 `2.0.0`。
- 人格管理新增独立版本字段，列表和编辑弹窗均可查看，客户端不再允许手工输入三类资源版本。
- AI 员工新增包版本，按员工版本、人格版本和全部关联 Skills 版本逐位累加，例如 `1.0.3 + 1.0.7 + 2.6.5 = 4.6.15`。
- 编辑 AI 员工、人格或任一直接/间接关联 Skill 后，会重新计算受影响 AI 员工的包版本。
- 市场目录、许可证安装快照和客户平台更新检测统一使用包版本；员工自身版本仍用于运营端维护记录。
- 受许可证保护的市场安装包接口始终读取当前运营端商品、人格和 Skills 快照，支持客户平台更新已购买员工的实际运行配置。

### Skill 交付与运行维护

- 客户平台可使用当前许可证下载已购 AI 员工的完整安装包，包内包含当次许可证快照中的人格、Skills、系统提示词、允许工具和确认策略，不暴露未授权商品配置。
- 内置 BOM 报价 Skill，支持从 Excel BOM 整理询价数据、查询立创商城等供应商报价、按库存、MOQ、交期和单价排序，并生成报价工作表。
- Skill ZIP 导入后的内容保存到可配置的持久化 Skills 目录，容器升级后仍可供已交付 AI 员工使用。
- 本地开发启动脚本会先停止占用 API 与管理台端口的旧进程，记录 API 标准输出和错误日志，并在管理台退出时清理本次启动的 API 进程。
- Docker 部署统一使用 `yaya-operation-center` 容器、数据卷和环境变量命名，同时保留旧许可证中心环境变量和既有部署迁移兼容。

### AI 员工交易与累计授权

- 新增受平台当前许可证保护的 AI 员工测试购买接口，使用 JWT subject 识别稳定 `customerId`，不接受客户端提交客户名称或授权主体。
- 测试购买自动创建客户订单、登记全额测试回款、生成回款流水并签发订单许可证，订单最终进入已交付状态。
- 新订单会继承客户最新有效许可证中的平台、模块和其他 AI 员工权益，再叠加本次购买，避免更新签名后丢失历史授权。
- 旧许可证继续保留用于审计；许可证状态接口仅返回同一主体的最新有效签名，由客户平台确认后应用。
- 测试支付接口不再依赖环境变量开关，保留接口供验收使用；后续上线时移除前端测试按钮。

### 请求审计日志

- 新增“日志信息”一级导航和 `/logs` 页面，支持全部、调试、信息、警告、错误五类筛选。
- 日志记录请求时间、日志级别、来源、客户端 IP、HTTP 方法、接口路径、状态码、响应耗时和 User-Agent。
- 支持按 IP、接口、方法、状态码和 User-Agent 搜索，便于判断请求是否符合预期。
- 请求级别按结果自动归类：成功 GET 为调试，成功写操作为信息，4xx 为警告，5xx 为错误。
- 日志接口受管理员认证保护，默认返回最近 300 条，前端加载最近 500 条记录。
- 日志最多保留 90 天、50,000 条，达到清理周期后自动删除过期和超量记录。
- 明确不保存请求体、Authorization、Cookie、许可证 JWT 和管理员令牌；客户端 IP 使用底层连接地址，不信任可伪造的转发头。
- 日志来源合并展示接口中文标题、HTTP 方法和路径，移除重复的请求接口列。
- 过滤运营管理台自身发起的内部 API 请求，保留外部平台校验、激活和异常访问等有审计价值的请求。
- 新增日志详情弹窗，集中展示 IP、User-Agent、接口、状态码、耗时和脱敏后的 JSON 返回结果。
- JSON 返回结果最多保存 32 KB，许可证正文、Token、Cookie、密码、API Key 和密钥字段会递归脱敏。

### 订单与部署类型

- 订单新增和编辑支持选择 SaaS 或本地部署，只有未回款订单允许编辑。
- 订单商品按平台模块、模块、AI 员工和服务显示不同颜色的 HeroUI 标签。
- 平台部署类型随订单写入许可证，供客户平台控制本地部署专属配置能力。

### AI 员工市场

- 新增只读公开目录 `GET /api/market/ai-employees`，供客户平台展示运营端已上架的 AI 员工。
- 公开目录仅返回商品展示、价格、计费周期和版本字段，不暴露人格、Skills、系统提示词或运营配置。

## 0.1.1 - 2026-08-06

### 许可证与授权

- 许可证签发继续强制关联已回款订单，客户主体、模块、AI 员工和授权期限全部从订单读取。
- 许可证状态接口会按稳定客户主体发现最新的已关联、未销毁、未过期许可证，供客户平台自动同步签名。
- 保留 `legacy_unlinked` 历史许可证只读兼容，不参与新的自动同步。
- 最新许可证完成主体、签名和有效期校验后，可通过激活接口更新运行状态。
- 签发接口只接收 `orderId` 和必要激活信息，禁止全局签发和不关联订单入口。
- `licenses.order_id` 强制 `NOT NULL + UNIQUE + FOREIGN KEY`，确保客户 → 订单 → 许可证关系不可绕过。
- 许可证载荷使用稳定 `customerId` 作为 JWT subject，并保存客户名称快照供运营端展示。
- 模块名称使用运营定义的 `title`，模块和 AI 员工均返回独立到期时间与运行状态。
- 许可证销毁、历史无订单许可证和未回款订单均有明确的只读或禁止操作边界。

### 运营管理台

- 运营管理台使用 HeroUI 组件生态，覆盖订单、许可证、客户、财务、AI 员工、人格和 Skills 管理页面。
- AI 员工商品支持人格与 Skills 封装、允许工具分组配置和 ZIP 导入。
- 订单中心支持客户选择、商品明细、单价、数量、授权天数、预计回款日期、分笔回款和订单取消。
- 客户管理支持客户档案、联系人信息、启用/停用和订单关联统计。
- 财务管理提供订单总额、累计回款、本月回款、待收金额、已回款订单和收款流水。
- AI 员工管理支持展示名称、分类、价格、计费周期、版本、人格绑定、封装 Skills 和系统提示词。
- 人格管理支持系统提示词和默认 Skills；Skills 支持版本、来源、包路径、允许工具、风险级别、确认策略和 ZIP 导入。
- 许可证页面展示客户快照、订单关联、平台有效期、模块有效期、AI 员工有效期和历史未关联状态。

### 前端与部署

- 管理台项目名称统一为 Yaya Operation Center，页面标题和品牌文案不再使用 license-center。
- 管理台依赖切换到 HeroUI、HeroUI Styles、Tailwind CSS 4 和 Gravity UI Icons。
- 主要表格、卡片、弹窗、选择器、复选框、确认框、Toast 和 Tooltip 使用 HeroUI 组件。
- 样式收敛到 HeroUI 语义主题和必要布局规则，移除表格、弹窗及组件级的大量自定义覆盖。
- 保留 Docker Compose、SQLite 数据卷、密钥分离和旧版 `LICENSE_CENTER_*` 环境变量兼容。

### 验证

- Rust `cargo check`、许可证关系测试和平台端后端测试通过。
